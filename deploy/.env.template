# Keymaker Environment Configuration
# Copy to /home/ridgetop/keymaker/.env on VPS

# ===========================================
# LLM Provider Configuration
# ===========================================

# Provider selection: 'ollama' (local, slow) or 'groq' (cloud, fast)
# Default: ollama
LLM_PROVIDER=groq

# Groq API (required if LLM_PROVIDER=groq)
# Get your API key at: https://console.groq.com/keys
GROQ_API_KEY=your-groq-api-key-here

# Groq model selection (optional, defaults shown)
# Options: llama-3.3-70b-versatile (best), llama-3.1-8b-instant (fastest)
# Vision: meta-llama/llama-4-scout-17b-16e-instruct, meta-llama/llama-4-maverick-17b-128e-instruct
GROQ_MODEL=llama-3.3-70b-versatile

# Ollama configuration (always needed for embeddings, generation if LLM_PROVIDER=ollama)
OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2:3b
# OLLAMA_EMBED_MODEL=nomic-embed-text
# OLLAMA_THREADS=4
# OLLAMA_KEEP_ALIVE=5m
# OLLAMA_CTX=4096

# ===========================================
# Application Configuration
# ===========================================

# Required: Auth token for API access
# Generate with: openssl rand -hex 32
KEYMAKER_TOKEN=your-secure-token-here

# Optional: Custom port (default 3001)
PORT=3001

# Environment: production or development
KEYMAKER_ENV=production

# Database (auto-configured via KEYMAKER_ENV, or override)
# DATABASE_URL=postgresql://user:pass@localhost:5432/keymaker_production

# ===========================================
# Feature Flags
# ===========================================

# Enable automatic entity extraction on every observe
# Set to 1 to extract people, projects, commitments, beliefs automatically
# KEYMAKER_EXTRACT=1
